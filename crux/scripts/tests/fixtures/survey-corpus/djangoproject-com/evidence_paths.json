{
  "why": "build_plan refuses a ratify row whose evidence path does not resolve to a file inside the repository root, so a hermetic test driving this fixture materializes each of these paths as a stub under its temp root first. Without it every one of the 40 rows refuses and the test measures the refusal, not the publish.",
  "paths": [
    "accounts/management/commands/delete_unactivated_users.py",
    "accounts/models.py",
    "accounts/urls.py",
    "aggregator/models.py",
    "djangoproject/hosts.py",
    "djangoproject/middleware.py",
    "djangoproject/settings/common.py",
    "djangoproject/settings/prod.py",
    "djangoproject/urls/www.py",
    "pyproject.toml"
  ]
}
